export type ICarType = {
    id: number,
    city: string
    year: number,
    brand: string,
    model: string,
    version: string,
    price: number,
    mileage: number,
}

export type IFilterType = {
    id: string,
    title: string
}